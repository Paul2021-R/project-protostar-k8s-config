# ---------------------------------------------------
# 1. Kustomize / Helm 생성 파일 (Generated Artifacts)
# ---------------------------------------------------

# Kustomize 빌드 결과 (스크립트 등으로 생성했을 경우)
manifests.yaml
kustomize/
kustomize.yaml.bak

# Helm 차트 다운로드 (Kustomize가 캐싱하거나 임시로 다운로드할 수 있는 경로)
# Charts 폴더는 일반적으로 소스 코드를 담는 용도가 아니라면 무시합니다.
charts/

# Helm 내부 캐시 및 템플릿 임시 파일
.helm/
*.tgz
*.tar.gz

# ---------------------------------------------------
# 2. Kubernetes / 로컬 환경 파일 (Local Cache)
# ---------------------------------------------------

# kubectl/Kustomize 캐시
.kube/
*.bak

# ---------------------------------------------------
# 3. IDE / OS 특정 파일 (Visual Studio Code, macOS)
# ---------------------------------------------------

.DS_Store
*.swp
.vscode/

# ---------------------------------------------------
# 4. Jenkins/CI 관련 파일 (만약 설정 파일을 저장했다면)
# ---------------------------------------------------
jenkins-home/
secrets/